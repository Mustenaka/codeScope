# codeScope 执行提示词集（正式版落地）
> 更新日期：2026-03-19
> 用途：后续让 AI 代理继续落地正式版时，统一输入边界并减少方向漂移

## 0. 通用总提示词

```text
你现在是这个项目的资深工程代理。

在开始任何分析、设计、改动之前，你必须先完整阅读并严格遵守以下文档：

- D:\Work\Code\Cross\codeScope\doc\全局修改指南-AI-Agent协作基线.md
- D:\Work\Code\Cross\codeScope\doc\重构方案-项目线程消息与P2P直连.md
- D:\Work\Code\Cross\codeScope\doc\bridge\Bridge旁路采集需求重设计.md
- D:\Work\Code\Cross\codeScope\doc\bridge-server-完整协议草案.md
- D:\Work\Code\Cross\codeScope\doc\正式版本落地路线图.md

同时参考：

- D:\Work\Code\Cross\codeScope\README.md
- D:\Work\Code\Cross\codeScope\CHANGELOG.md
- D:\Work\Code\Cross\codeScope\bridge\README.md

总原则：

1. 以当前代码真实实现为准
2. 不要把 bridge 改回托管启动模式
3. 不要把移动端默认做成 session/event 调试面板
4. 后续实现优先围绕：
   - device
   - project
   - thread
   - message
   - thread_state
   - file browser
   - p2p direct / relay fallback
5. server 的默认身份是：
   - P2P discovery service
   - route broker
   - optional relay fallback
   而不是默认内容中心
6. 涉及消息正文、线程历史、文件内容时，优先考虑 bridge <-> mobile 直接传输
7. heartbeat、原始命令行、bridge observing 提示、底层 payload 只属于 debug 视图，不应成为 release 主视图

输出时必须明确区分：
- 已实现
- 半实现
- 未实现

如果需求跨多个子系统，必须先输出：
1. 需求理解
2. 当前现状
3. 受影响模块
4. 修改方案
5. 需要同步修改的文件/目录
6. 风险与回归点
7. 建议实施顺序
```

## 1. Bridge 落地提示词

```text
请在 D:\Work\Code\Cross\codeScope\bridge 中继续实现正式版 bridge。

重点目标：
- 保持 side-channel collector，不回到托管启动
- 继续完善 project/thread/message/thread_state 的真实采集
- 为 Codex 和 Claude Code 分别建立 capture adapter
- 保证 legacy session/event 兼容链路继续可用
- 为后续 p2p endpoint 预留位置

优先检查：
- bridge/internal/discovery/
- bridge/internal/capture/
- bridge/internal/session/
- bridge/internal/transport/

必须回答：
1. 当前哪些是已实现、半实现、未实现
2. 真实 message 采集来自哪里
3. thread_id 和 source_session_id 如何区分
4. 哪些事件只能算 debug，不得进入 release 主视图
5. 本轮修改如何避免把一个真实 thread 拆成多个兼容 session
```

## 2. Server 落地提示词

```text
请在 D:\Work\Code\Cross\codeScope\server 中继续实现正式版 server。

目标不是继续扩展中心化 session/event 平台，而是：
- device registry
- project/thread index
- thread/message read model
- p2p route negotiation
- relay fallback
- legacy session/event compatibility

优先检查：
- server/internal/project/
- server/internal/event/
- server/internal/http/
- server/internal/filebrowser/
- server/internal/session/

必须回答：
1. 当前 server 哪些职责已经承担，哪些还只是兼容层
2. release 主读模型是否已经脱离 session/event 视角
3. thread_state 是真实状态机还是启发式推导
4. 哪些正文内容未来应从 server 中心存储迁出到 p2p
5. 本轮变更如何验证不会让 mobile 又退回 event viewer
```

## 3. Mobile 落地提示词

```text
请在 D:\Work\Code\Cross\codeScope\mobile 中继续实现正式版 mobile。

目标：
- 默认主视图始终围绕 project -> thread -> message
- file browser 按 project 语义接入
- prompt 输入按 thread/project 正式闭环
- debug 信息只放到诊断视图，不污染 release 主界面

优先检查：
- mobile/lib/modules/project/
- mobile/lib/modules/thread/
- mobile/lib/modules/message/
- mobile/lib/modules/file/
- mobile/lib/modules/prompt/
- mobile/lib/modules/log/ 与 mobile/lib/modules/session/ 仅作为兼容/调试层理解

必须回答：
1. 当前哪些页面还是兼容层残留
2. 哪些 UI 仍在暴露 session/event 心智
3. thread detail 是否支持长消息折叠、滚动、状态查看和后续 prompt 输入
4. release 视图与 debug 视图是否已经分离
5. 断线重连后，列表和详情的实时同步链路如何恢复
```

## 4. 协议 / P2P 落地提示词

```text
请围绕 D:\Work\Code\Cross\codeScope\doc\bridge-server-完整协议草案.md 和当前代码，推进正式版协议实现。

目标：
- server 默认做 discovery / route broker
- bridge <-> mobile 优先 p2p direct
- 无法直连时再走 relay fallback
- project/thread/message/file 内容层与索引层分离

必须回答：
1. 当前协议里哪些对象已存在，哪些只是草案
2. route_request / route_offer / answer / relay_required 如何落到现有代码
3. 哪些内容必须走 p2p，哪些可以只留摘要在 server
4. 安全、鉴权、NAT、重连、回放如何处理
5. 与现有 legacy ws/REST 接口如何兼容迁移
```

## 5. 文档治理提示词

```text
请检查 D:\Work\Code\Cross\codeScope\doc 及相关 README/CHANGELOG，完成文档治理。

目标：
- 删除重复、误导、与当前实现不符的文档
- 合并重复表达，减少多份文档描述同一层约束
- 明确哪些是基线文档、哪些是兼容层文档、哪些是联调文档
- 所有文档都必须明确区分：已实现 / 半实现 / 未实现

检查重点：
1. 是否还有把 mobile 描述成 session/event viewer 的文档
2. 是否还有把 server 描述成默认内容中心的文档
3. 是否还有把 bridge 描述成托管启动器的文档
4. 是否还有已经被新基线完全覆盖的重复文档或旧提示词
5. 文档索引、README、CHANGELOG 是否同步
```

## 6. 联调 / 测试提示词

```text
请在不改变当前产品方向的前提下，为 codeScope 补齐正式版联调与验证方案。

要求：
- 不以 mobile 是否完成作为唯一验证入口
- 兼容层联调与正式版主链路联调分开写
- 对每个阶段明确：
  - 目标
  - 输入
  - 预期事件
  - 预期 project/thread/message/thread_state 结果
  - 回归风险

必须覆盖：
- bridge discovery
- 真实 message capture
- server project/thread 聚合
- mobile realtime subscription
- prompt 回流
- p2p direct / relay fallback
```

## 一句话结论

后续所有 AI 代理都应被强制约束在同一条主线上：把 codeScope 做成一个面向 `project / thread / message / thread_state` 的移动端远程协作系统，而不是继续修补一个 `session/event` 调试面板。
