# Store Package

This package will host persistence adapters for:

- sessions
- events
- prompts
- command tasks

For the first implementation pass, prefer SQLite-backed repositories with narrow interfaces per module.

